org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Built-in Kotlin: od AGP 9 wtyczkę Kotlina dostarcza sam AGP, zamiast każdego
# modułu z osobna. Flaga wstawiona przez migrator Fluttera jako `false`; włączona
# w chore/037, gdy obie wtyczki blokujące migrację (package_info_plus,
# print_bluetooth_thermal) doczekały się wersji bez własnego KGP.
android.builtInKotlin=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
